Upload these two files to the Supabase Storage bucket `kop-surat`
(public) — see README.md §3.4. They're the same images already embedded
as base64 in the standalone `index.html` prototype's letter generator.
